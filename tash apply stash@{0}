[1mdiff --git a/.gitignore b/.gitignore[m
[1mindex c2a92d7..57d9244 100644[m
[1m--- a/.gitignore[m
[1m+++ b/.gitignore[m
[36m@@ -8,3 +8,14 @@[m [mFRONT/FORMS/generated/*.pdf[m
 [m
 # Scratch / debug / temp artifacts[m
 *.log[m
[32m+[m[32mprobe-mp4.js[m
[32m+[m[32mchroma-math.test.js[m
[32m+[m[32mqa-screenshot.js[m
[32m+[m[32mqa-embed.js[m
[32m+[m[32mchrome-profile/[m
[32m+[m[32mqa-results/[m
[32m+[m[32mplaceholder-x.txt[m
[32m+[m[32mnode_modules/[m
[32m+[m[32mpackage.json[m
[32m+[m[32mpackage-lock.json[m
[32m+[m[32mFRONT/scslaicares-qa.html[m
[1mdiff --git a/.vscode/launch.json b/.vscode/launch.json[m
[1mindex dca1555..a74e813 100644[m
[1m--- a/.vscode/launch.json[m
[1m+++ b/.vscode/launch.json[m
[36m@@ -9,7 +9,8 @@[m
             "request": "launch",[m
             "name": "Launch Chrome against localhost",[m
             "url": "http://localhost:5500/FRONT",[m
[31m-            "webRoot": "${workspaceFolder}/FRONT"[m
[32m+[m[32m            "webRoot": "${workspaceFolder}/FRONT",[m
[32m+[m[32m            "sourceMaps": false[m
             [m
         }[m
     ][m
[1mdiff --git a/FRONT/script.js b/FRONT/script.js[m
[1mindex e05e4cc..a6d28d9 100644[m
[1m--- a/FRONT/script.js[m
[1m+++ b/FRONT/script.js[m
[36m@@ -196,6 +196,332 @@[m [mlet flipbookElement = document.getElementById('flipbook');[m
 const closeReaderButtons = document.querySelectorAll('[data-close-reader]');[m
 const bookOpening = document.getElementById('book-opening');[m
 const bookOpeningLabel = document.querySelector('[data-book-opening-label]');[m
[32m+[m[32mconst bookOpeningVideo = document.getElementById('book-opening-video');[m
[32m+[m[32m// ===========================[m
[32m+[m[32m// Green-screen chroma key for the book-opening animation[m
[32m+[m[32m// ===========================[m
[32m+[m[32m//[m
[32m+[m[32m// The book-opening video ("animation/マエット (Maetto).mp4") is recorded against[m
[32m+[m[32m// a green screen. A plain <video> element cannot display transparency, so each[m
[32m+[m[32m// frame is copied onto a single reused canvas and the green background is keyed[m
[32m+[m[32m// out in real time (alpha = 0) before the frame is presented. The exact shade[m
[32m+[m[32m// of green is sampled from the video itself instead of assuming pure #00FF00,[m
[32m+[m[32m// a hue tolerance plus a soft feather band keeps the edges free of hard halos,[m
[32m+[m[32m// and green spill is suppressed along those softened edges. Neutral (paper,[m
[32m+[m[32m// white) and near-black pixels are always kept so the book itself stays solid.[m
[32m+[m
[32m+[m[32mconst CHROMA_KEY_CONFIG = {[m
[32m+[m[32m  hueTolerance: 20,          // degrees around the sampled hue that are fully removed[m
[32m+[m[32m  hueFeather: 24,            // soft falloff band that prevents harsh halos[m
[32m+[m[32m  despillRange: 30,          // extra band where green spill is neutralized on kept pixels[m
[32m+[m[32m  saturationFloor: 0.18,     // low-saturation (neutral) pixels are always kept[m
[32m+[m[32m  valueFloor: 0.10,          // near-black pixels are always kept[m
[32m+[m[32m  sampleHueRange: [70, 170], // hues counted as "green-ish" while sampling[m
[32m+[m[32m  minSamplePixels: 200,      // green samples required before the sample is trusted[m
[32m+[m[32m  fallbackKeyHue: 120        // used until (and unless) the video itself is sampled[m
[32m+[m[32m};[m
[32m+[m
[32m+[m[32mfunction hueDistanceTo(hue, keyHue) {[m
[32m+[m[32m  const diff = Math.abs(hue - keyHue) % 360;[m
[32m+[m[32m  return diff > 180 ? 360 - diff : diff;[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32mfunction computeHue(r, g, b, max, delta) {[m
[32m+[m[32m  if (delta === 0) return 0;[m
[32m+[m[32m  let hue;[m
[32m+[m[32m  if (max === r) hue = 60 * (((g - b) / delta) % 6);[m
[32m+[m[32m  else if (max === g) hue = 60 * ((b - r) / delta + 2);[m
[32m+[m[32m  else hue = 60 * ((r - g) / delta + 4);[m
[32m+[m[32m  return hue < 0 ? hue + 360 : hue;[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32mfunction createBookOpeningChromaKey(video) {[m
[32m+[m[32m  if (!video) {[m
[32m+[m[32m    return { start() {}, stop() {} };[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  const supportsVideoFrameCallback = typeof video.requestVideoFrameCallback === 'function';[m
[32m+[m[32m  const state = {[m
[32m+[m[32m    canvas: null,[m
[32m+[m[32m    ctx: null,[m
[32m+[m[32m    overlay: null,[m
[32m+[m[32m    active: false,[m
[32m+[m[32m    usingFallback: false,[m
[32m+[m[32m    frameHandle: 0,[m
[32m+[m[32m    rafId: 0,[m
[32m+[m[32m    keyHue: CHROMA_KEY_CONFIG.fallbackKeyHue,[m
[32m+[m[32m    hasSampledKey: false,[m
[32m+[m[32m    lastVideoTime: -1,[m
[32m+[m[32m    resizeHandler: null[m
[32m+[m[32m  };[m
[32m+[m
[32m+[m[32m  function ensureCanvas() {[m
[32m+[m[32m    if (state.canvas) return true;[m
[32m+[m[32m    const panel = video.parentElement;[m
[32m+[m[32m    if (!panel) return false;[m
[32m+[m
[32m+[m[32m    // One canvas for the whole session; reused on every opening.[m
[32m+[m[32m    const canvas = document.createElement('canvas');[m
[32m+[m[32m    canvas.className = 'book-opening-canvas';[m
[32m+[m[32m    canvas.setAttribute('aria-hidden', 'true');[m
[32m+[m[32m    panel.appendChild(canvas);[m
[32m+[m
[32m+[m[32m    state.canvas = canvas;[m
[32m+[m[32m    state.ctx = canvas.getContext('2d', { alpha: true, willReadFrequently: true });[m
[32m+[m[32m    return Boolean(state.ctx);[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  function syncCanvasSize() {[m
[32m+[m[32m    if (!state.canvas) return;[m
[32m+[m[32m    const width = video.videoWidth;[m
[32m+[m[32m    const height = video.videoHeight;[m
[32m+[m[32m    if (!width || !height) return;[m
[32m+[m[32m    // Backing store matches the video's intrinsic size, so the aspect ratio is[m
[32m+[m[32m    // preserved exactly; CSS (object-fit: contain) handles responsive display.[m
[32m+[m[32m    if (state.canvas.width !== width || state.canvas.height !== height) {[m
[32m+[m[32m      state.canvas.width = width;[m
[32m+[m[32m      state.canvas.height = height;[m
[32m+[m[32m    }[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  function readFramePixels() {[m
[32m+[m[32m    try {[m
[32m+[m[32m      return state.ctx.getImageData(0, 0, state.canvas.width, state.canvas.height);[m
[32m+[m[32m    } catch (error) {[m
[32m+[m[32m      // Pixel access failed (e.g. tainted canvas) - fall back to the raw video.[m
[32m+[m[32m      return null;[m
[32m+[m[32m    }[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32mfunction sampleKeyColorFromEdges(imageData) {[m
[32m+[m[32m    const { data } = imageData;[m
[32m+[m[32m    const width = state.canvas.width;[m
[32m+[m[32m    const height = state.canvas.height;[m
[32m+[m[32m    const [hueMin, hueMax] = CHROMA_KEY_CONFIG.sampleHueRange;[m
[32m+[m[32m    const band = Math.max(2, Math.floor(Math.min(width, height) * 0.1));[m
[32m+[m[32m    const step = Math.max(2, Math.floor(Math.min(width, height) / 40));[m
[32m+[m
[32m+[m[32m    let sumCos = 0;[m
[32m+[m[32m    let sumSin = 0;[m
[32m+[m[32m    let samples = 0;[m
[32m+[m
[32m+[m[32m    const consider = (x, y) => {[m
[32m+[m[32m      const index = (y * width + x) * 4;[m
[32m+[m[32m      if (data[index + 3] === 0) return;[m
[32m+[m[32m      const r = data[index] / 255;[m
[32m+[m[32m      const g = data[index + 1] / 255;[m
[32m+[m[32m      const b = data[index + 2] / 255;[m
[32m+[m
[32m+[m[32m      const max = Math.max(r, g, b);[m
[32m+[m[32m      const min = Math.min(r, g, b);[m
[32m+[m[32m      const delta = max - min;[m
[32m+[m[32m      if (max === 0 || max < 0.06 || delta / max < 0.15) return;[m
[32m+[m
[32m+[m[32m      const hue = computeHue(r, g, b, max, delta);[m
[32m+[m[32m      if (hue < hueMin || hue > hueMax) return;[m
[32m+[m
[32m+[m[32m      // Circular mean so a hue near 0/360 cannot skew the average.[m
[32m+[m[32m      const radians = (hue * Math.PI) / 180;[m
[32m+[m[32m      sumCos += Math.cos(radians);[m
[32m+[m[32m      sumSin += Math.sin(radians);[m
[32m+[m[32m      samples += 1;[m
[32m+[m[32m    };[m
[32m+[m
[32m+[m[32m    for (let x = 0; x < width; x += step) {[m
[32m+[m[32m      for (let y = 0; y < band; y += step) {[m
[32m+[m[32m        consider(x, y);[m
[32m+[m[32m        consider(x, height - 1 - y);[m
[32m+[m[32m      }[m
[32m+[m[32m    }[m
[32m+[m[32m    for (let y = 0; y < height; y += step) {[m
[32m+[m[32m      for (let x = 0; x < band; x += step) {[m
[32m+[m[32m        consider(x, y);[m
[32m+[m[32m        consider(width - 1 - x, y);[m
[32m+[m[32m      }[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    if (samples < CHROMA_KEY_CONFIG.minSamplePixels) return false;[m
[32m+[m
[32m+[m[32m    const meanHue = (Math.atan2(sumSin, sumCos) * 180) / Math.PI;[m
[32m+[m[32m    state.keyHue = meanHue < 0 ? meanHue + 360 : meanHue;[m
[32m+[m[32m    return true;[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  function keyOutGreenPixels(imageData) {[m
[32m+[m[32m    const data = imageData.data;[m
[32m+[m[32m    const { hueTolerance, hueFeather, despillRange, saturationFloor, valueFloor } = CHROMA_KEY_CONFIG;[m
[32m+[m[32m    const innerBound = hueTolerance;[m
[32m+[m[32m    const outerBound = hueTolerance + hueFeather;[m
[32m+[m[32m    const despillBound = outerBound + despillRange;[m
[32m+[m
[32m+[m[32m    for (let i = 0; i < data.length; i += 4) {[m
[32m+[m[32m      const r = data[i] / 255;[m
[32m+[m[32m      const g = data[i + 1] / 255;[m
[32m+[m[32m      const b = data[i + 2] / 255;[m
[32m+[m
[32m+[m[32m      const max = Math.max(r, g, b);[m
[32m+[m[32m      const min = Math.min(r, g, b);[m
[32m+[m[32m      const delta = max - min;[m
[32m+[m[32m      const saturation = max === 0 ? 0 : delta / max;[m
[32m+[m
[32m+[m[32m      // Keep neutral tones (paper, white pages) and near-black detail opaque.[m
[32m+[m[32m      if (saturation < saturationFloor || max < valueFloor) continue;[m
[32m+[m
[32m+[m[32m      const hue = computeHue(r, g, b, max, delta);[m
[32m+[m[32m      const distance = hueDistanceTo(hue, state.keyHue);[m
[32m+[m
[32m+[m[32m      if (distance <= innerBound) {[m
[32m+[m[32m        // Solid background: fully transparent (alpha = 0), never recolored.[m
[32m+[m[32m        data[i + 3] = 0;[m
[32m+[m[32m        continue;[m
[32m+[m[32m      }[m
[32m+[m[32m      if (distance >= outerBound) {[m
[32m+[m[32m        // Just outside the feather: foreground blends that still carry a green[m
[32m+[m[32m        // cast (e.g. hair mixed with the screen). Neutralize the spill so no[m
[32m+[m[32m        // green halo remains, but keep the pixel itself fully visible.[m
[32m+[m[32m        if (distance < despillBound && g > r && g > b) {[m
[32m+[m[32m          data[i + 1] = Math.round(Math.max(r, b) * 255);[m
[32m+[m[32m        }[m
[32m+[m[32m        continue;[m
[32m+[m[32m      }[m
[32m+[m
[32m+[m[32m      // Feathered edge: soften the mix and pull the green spill back so the[m
[32m+[m[32m      // boundary does not glow green against the page behind it.[m
[32m+[m[32m      const alpha = Math.round(((distance - innerBound) / hueFeather) * 255);[m
[32m+[m[32m      if (alpha <= 0) {[m
[32m+[m[32m        data[i + 3] = 0;[m
[32m+[m[32m        continue;[m
[32m+[m[32m      }[m
[32m+[m[32m      if (g > r && g > b) {[m
[32m+[m[32m        data[i + 1] = Math.round(Math.max(r, b) * 255);[m
[32m+[m[32m      }[m
[32m+[m[32m      data[i + 3] = alpha;[m
[32m+[m[32m    }[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  function detachFrameLoop() {[m
[32m+[m[32m    if (state.rafId) {[m
[32m+[m[32m      window.cancelAnimationFrame(state.rafId);[m
[32m+[m[32m      state.rafId = 0;[m
[32m+[m[32m    }[m
[32m+[m[32m    if (state.frameHandle && supportsVideoFrameCallback) {[m
[32m+[m[32m      video.cancelVideoFrameCallback?.(state.frameHandle);[m
[32m+[m[32m      state.frameHandle = 0;[m
[32m+[m[32m    }[m
[32m+[m[32m    if (state.resizeHandler) {[m
[32m+[m[32m      video.removeEventListener('resize', state.resizeHandler);[m
[32m+[m[32m      state.resizeHandler = null;[m
[32m+[m[32m    }[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  function disableChromaProcessing() {[m
[32m+[m[32m    state.usingFallback = true;[m
[32m+[m[32m    state.active = false;[m
[32m+[m[32m    detachFrameLoop();[m
[32m+[m[32m    // Real-time keying is unavailable; the plain video element takes over so[m
[32m+[m[32m    // the opening animation still plays.[m
[32m+[m[32m    state.overlay?.classList.remove('is-chroma');[m
[32m+[m[32m    state.overlay?.classList.add('is-fallback');[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  function processFrame() {[m
[32m+[m[32m    if (!state.active || state.usingFallback) return;[m
[32m+[m[32m    syncCanvasSize();[m
[32m+[m[32m    if (!state.ctx || !state.canvas.width || !state.canvas.height) return;[m
[32m+[m
[32m+[m[32m    try {[m
[32m+[m[32m      state.ctx.drawImage(video, 0, 0, state.canvas.width, state.canvas.height);[m
[32m+[m[32m    } catch (error) {[m
[32m+[m[32m      disableChromaProcessing();[m
[32m+[m[32m      return;[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    const imageData = readFramePixels();[m
[32m+[m[32m    if (!imageData) {[m
[32m+[m[32m      disableChromaProcessing();[m
[32m+[m[32m      return;[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    if (!state.hasSampledKey) {[m
[32m+[m[32m      // Learn the actual green shade from this first frame before keying it.[m
[32m+[m[32m      sampleKeyColorFromEdges(imageData);[m
[32m+[m[32m      state.hasSampledKey = true;[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    keyOutGreenPixels(imageData);[m
[32m+[m
[32m+[m[32m    try {[m
[32m+[m[32m      state.ctx.putImageData(imageData, 0, 0);[m
[32m+[m[32m    } catch (error) {[m
[32m+[m[32m      disableChromaProcessing();[m
[32m+[m[32m    }[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  function scheduleNextFrame() {[m
[32m+[m[32m    if (!state.active || state.usingFallback) return;[m
[32m+[m[32m    if (supportsVideoFrameCallback) {[m
[32m+[m[32m      state.frameHandle = video.requestVideoFrameCallback(() => {[m
[32m+[m[32m        if (!state.active || state.usingFallback) return;[m
[32m+[m[32m        processFrame();[m
[32m+[m[32m        scheduleNextFrame();[m
[32m+[m[32m      });[m
[32m+[m[32m      return;[m
[32m+[m[32m    }[m
[32m+[m[32m    state.rafId = window.requestAnimationFrame(() => {[m
[32m+[m[32m      if (!state.active || state.usingFallback) return;[m
[32m+[m[32m      const currentTime = video.currentTime;[m
[32m+[m[32m      if (currentTime !== state.lastVideoTime) {[m
[32m+[m[32m        state.lastVideoTime = currentTime;[m
[32m+[m[32m        processFrame();[m
[32m+[m[32m      }[m
[32m+[m[32m      scheduleNextFrame();[m
[32m+[m[32m    });[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  function start() {[m
[32m+[m[32m    if (state.active) return;[m
[32m+[m[32m    state.overlay = video.closest('.book-opening');[m
[32m+[m
[32m+[m[32m    if (state.usingFallback) {[m
[32m+[m[32m      state.overlay?.classList.remove('is-chroma');[m
[32m+[m[32m      state.overlay?.classList.add('is-fallback');[m
[32m+[m[32m      return;[m
[32m+[m[32m    }[m
[32m+[m[32m    if (!ensureCanvas()) {[m
[32m+[m[32m      disableChromaProcessing();[m
[32m+[m[32m      return;[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    state.active = true;[m
[32m+[m[32m    state.hasSampledKey = false;[m
[32m+[m[32m    state.lastVideoTime = -1;[m
[32m+[m[32m    syncCanvasSize();[m
[32m+[m
[32m+[m[32m    state.overlay?.classList.remove('is-fallback');[m
[32m+[m[32m    state.overlay?.classList.add('is-chroma');[m
[32m+[m
[32m+[m[32m    state.resizeHandler = state.resizeHandler || (() => syncCanvasSize());[m
[32m+[m[32m    if (state.resizeHandler) {[m
[32m+[m[32m      video.removeEventListener('resize', state.resizeHandler);[m
[32m+[m[32m    }[m
[32m+[m[32m    video.addEventListener('resize', state.resizeHandler);[m
[32m+[m[32m    scheduleNextFrame();[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  function stop() {[m
[32m+[m[32m    state.active = false;[m
[32m+[m[32m    detachFrameLoop();[m
[32m+[m[32m    state.overlay?.classList.remove('is-chroma');[m
[32m+[m[32m    state.overlay?.classList.remove('is-fallback');[m
[32m+[m[32m    if (state.ctx && state.canvas) {[m
[32m+[m[32m      state.ctx.clearRect(0, 0, state.canvas.width, state.canvas.height);[m
[32m+[m[32m    }[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  return { start, stop };[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32mconst bookOpeningChromaKey = createBookOpeningChromaKey(bookOpeningVideo);[m
 [m
 const detailBindings = {[m
   activityTitle: document.querySelector('[data-activity-title]'),[m
[36m@@ -489,12 +815,73 @@[m [mfunction hasReducedMotionPreference() {[m
   return window.matchMedia?.('(prefers-reduced-motion: reduce)').matches;[m
 }[m
 [m
[32m+[m[32mfunction getBookOpeningDuration() {[m
[32m+[m[32m  if (hasReducedMotionPreference()) return Promise.resolve();[m
[32m+[m
[32m+[m[32m  return new Promise((resolve) => {[m
[32m+[m[32m    if (!bookOpeningVideo) return resolve();[m
[32m+[m
[32m+[m[32m    let settled = false;[m
[32m+[m[32m    let fallbackTimer = 0;[m
[32m+[m[32m    const finish = () => {[m
[32m+[m[32m      if (settled) return;[m
[32m+[m[32m      settled = true;[m
[32m+[m[32m      window.clearTimeout(fallbackTimer);[m
[32m+[m[32m      bookOpeningChromaKey?.stop();[m
[32m+[m[32m      resolve();[m
[32m+[m[32m    };[m
[32m+[m[32m    // Safety cap: prefer the video's real duration so the animation is never[m
[32m+[m[32m    // cut short; keep a fixed cap as a fallback if metadata is unavailable.[m
[32m+[m[32m    const duration = Number(bookOpeningVideo.duration);[m
[32m+[m[32m    const capMs = Number.isFinite(duration) && duration > 0 ? (duration + 3) * 1000 : 15000;[m
[32m+[m[32m    const startFallbackTimer = () => {[m
[32m+[m[32m      if (fallbackTimer) return;[m
[32m+[m[32m      fallbackTimer = window.setTimeout(finish, capMs);[m
[32m+[m[32m    };[m
[32m+[m
[32m+[m[32m    bookOpeningVideo.addEventListener('ended', finish, { once: true });[m
[32m+[m[32m    bookOpeningVideo.addEventListener('error', finish, { once: true });[m
[32m+[m[32m    bookOpeningVideo.addEventListener('loadedmetadata', startFallbackTimer, { once: true });[m
[32m+[m[32m    startFallbackTimer();[m
[32m+[m
[32m+[m[32m    // The tap/click that triggered the opening counts as user interaction, so[m
[32m+[m[32m    // keep the video's own audio when the browser allows it. If playback is[m
[32m+[m[32m    // refused, retry once muted so the animation still runs.[m
[32m+[m[32m    bookOpeningVideo.muted = false;[m
[32m+[m
[32m+[m[32m    try {[m
[32m+[m[32m      const playPromise = bookOpeningVideo.play();[m
[32m+[m[32m      if (playPromise && typeof playPromise.catch === 'function') {[m
[32m+[m[32m        playPromise.catch(() => {[m
[32m+[m[32m          if (settled) return;[m
[32m+[m[32m          bookOpeningVideo.muted = true;[m
[32m+[m[32m          try {[m
[32m+[m[32m            const retryPromise = bookOpeningVideo.play();[m
[32m+[m[32m            if (retryPromise && typeof retryPromise.catch === 'function') {[m
[32m+[m[32m              retryPromise.catch(finish);[m
[32m+[m[32m            }[m
[32m+[m[32m          } catch (error) {[m
[32m+[m[32m            finish();[m
[32m+[m[32m          }[m
[32m+[m[32m        });[m
[32m+[m[32m      }[m
[32m+[m[32m    } catch (error) {[m
[32m+[m[32m      finish();[m
[32m+[m[32m    }[m
[32m+[m[32m  });[m
[32m+[m[32m}[m
[32m+[m
 function cancelBookOpening() {[m
   if (!isBookOpening) return;[m
 [m
   bookOpeningSequence += 1;[m
   isBookOpening = false;[m
[32m+[m[32m  bookOpeningChromaKey?.stop();[m
   bookOpening?.classList.remove('is-playing');[m
[32m+[m[32m  if (bookOpeningVideo) {[m
[32m+[m[32m    bookOpeningVideo.pause();[m
[32m+[m[32m    bookOpeningVideo.currentTime = 0;[m
[32m+[m[32m  }[m
   if (bookOpening) {[m
     bookOpening.hidden = true;[m
     bookOpening.setAttribute('aria-hidden', 'true');[m
[36m@@ -540,17 +927,22 @@[m [masync function openReaderAfterBookOpening(index, startPage) {[m
   bookOpening.classList.remove('is-playing');[m
   void bookOpening.offsetWidth;[m
   bookOpening.classList.add('is-playing');[m
[32m+[m[32m  bookOpeningChromaKey?.start();[m
 [m
[31m-  const animationDuration = hasReducedMotionPreference() ? 0 : 5700;[m
[32m+[m[32m  if (bookOpeningVideo) {[m
[32m+[m[32m    bookOpeningVideo.currentTime = 0;[m
[32m+[m[32m  }[m
   await Promise.all([[m
     preloadBasePages(),[m
[31m-    new Promise((resolve) => window.setTimeout(resolve, animationDuration))[m
[32m+[m[32m    getBookOpeningDuration()[m
   ]);[m
 [m
   if (sequence !== bookOpeningSequence) return;[m
 [m
   isBookOpening = false;[m
[32m+[m[32m  bookOpeningChromaKey?.stop();[m
   bookOpening.classList.remove('is-playing');[m
[32m+[m[32m  if (bookOpeningVideo) bookOpeningVideo.pause();[m
   bookOpening.hidden = true;[m
   bookOpening.setAttribute('aria-hidden', 'true');[m
   await openReader(index, startPage);[m
[1mdiff --git a/FRONT/scslaicares.css b/FRONT/scslaicares.css[m
[1mindex 2e82eb4..4e7fc9b 100644[m
[1m--- a/FRONT/scslaicares.css[m
[1m+++ b/FRONT/scslaicares.css[m
[36m@@ -144,17 +144,13 @@[m [mbody, * {[m
 [m
 /* Opening sequence shown between selecting an edition and displaying its reader. */[m
 .book-opening {[m
[31m-    --book-cover-delay: 0.45s;[m
[31m-    --book-page-delay: 1.35s;[m
     position: fixed;[m
     z-index: 1450;[m
     inset: 0;[m
     display: grid;[m
[31m-    padding: 1rem;[m
[31m-    overflow: auto;[m
[32m+[m[32m    overflow: hidden;[m
     place-items: center;[m
[31m-    background: rgba(13, 11, 22, 0.68);[m
[31m-    backdrop-filter: blur(5px);[m
[32m+[m[32m    background: transparent;[m
 }[m
 [m
 .book-opening[hidden] {[m
[36m@@ -163,202 +159,99 @@[m [mbody, * {[m
 [m
 .book-opening-label {[m
     position: absolute;[m
[31m-    z-index: 1;[m
[31m-    top: calc(50% + 8.4rem);[m
[32m+[m[32m    z-index: 2;[m
     right: 1rem;[m
[32m+[m[32m    bottom: 0.9rem;[m
     left: 1rem;[m
     margin: 0;[m
[31m-    color: rgba(255, 255, 255, 0.82);[m
[32m+[m[32m    color: rgba(255, 255, 255, 0.92);[m
     font-size: 0.82rem;[m
     font-weight: 700;[m
     letter-spacing: 0.14em;[m
     text-align: center;[m
     text-transform: uppercase;[m
[32m+[m[32m    text-shadow: 0 1px 4px rgba(0, 0, 0, 0.75);[m
 }[m
 [m
 .book-opening-panel {[m
     position: relative;[m
[31m-    width: min(100%, 37rem);[m
[31m-    height: min(24rem, calc(100vh - 2rem));[m
[32m+[m[32m    width: 100%;[m
[32m+[m[32m    height: 100%;[m
     overflow: hidden;[m
[31m-    border: 1px solid rgba(255, 255, 255, 0.18);[m
[31m-    border-radius: 18px;[m
     background: transparent;[m
[31m-    box-shadow: 0 28px 70px rgba(0, 0, 0, 0.48);[m
 }[m
 [m
[31m-.book-opening .book {[m
[32m+[m[32m/* The opening sequence is the green-screen book-opening video. A canvas next[m
[32m+[m[32m   to the <video> receives every frame with the green screen keyed out to real[m
[32m+[m[32m   transparency (alpha = 0), so the page behind the overlay stays visible.[m
[32m+[m[32m   The raw <video> itself stays hidden: it is only the decode source, plus a[m
[32m+[m[32m   fallback renderer for browsers where per-pixel access is unavailable. */[m
[32m+[m[32m.book-opening-video {[m
     position: absolute;[m
[31m-    top: 0;[m
[31m-    right: 0;[m
[31m-    bottom: 0;[m
[31m-    left: 0;[m
[31m-    width: 18.5rem;[m
[31m-    height: 12.5rem;[m
[31m-    margin: auto;[m
[31m-    perspective: 70rem;[m
[31m-}[m
[31m-[m
[31m-/* Show the closed book first, then open its cover before turning pages. */[m
[31m-.book-opening.is-playing .book {[m
[31m-    animation: bookOpeningPrelude 0.45s cubic-bezier(0.22, 0.8, 0.3, 1) both;[m
[31m-}[m
[31m-[m
[31m-.book-opening .cover {[m
[31m-    width: 9.25rem;[m
[31m-    height: 12.5rem;[m
[31m-    background: linear-gradient(135deg, #7b4b2d 0%, #4a2818 62%, #2d160d 57%);[m
[31m-    transform: rotateY(0deg);[m
[31m-}[m
[31m-[m
[31m-.book-opening .page {[m
[31m-    top: 0.25rem;[m
[31m-    left: 0.25rem;[m
[31m-    width: 9rem;[m
[31m-    height: 12rem;[m
[31m-    color: #777;[m
[31m-    font-family: monospace !important;[m
[31m-    font-size: 8px;[m
[31m-    text-align: right;[m
[31m-    background-color: #e9e6c4;[m
[31m-    transform: rotateY(0deg);[m
[31m-}[m
[31m-[m
[31m-.book-opening .page::before,[m
[31m-.book-opening .page::after {[m
[32m+[m[32m    inset: 0;[m
[32m+[m[32m    z-index: 0;[m
     display: block;[m
[31m-    padding-bottom: 1rem;[m
[31m-    content: "";[m
[31m-    border-top: 1px dashed rgba(0, 0, 0, 0.3);[m
[31m-}[m
[31m-[m
[31m-.book-opening .cover,[m
[31m-.book-opening .page {[m
[31m-    position: absolute;[m
[31m-    padding: 1rem;[m
[31m-    box-sizing: border-box;[m
[31m-    border-radius: 5px 0 0 5px;[m
[31m-    box-shadow:[m
[31m-        inset 3px 0 20px rgba(0, 0, 0, 0.2),[m
[31m-        0 0 15px rgba(0, 0, 0, 0.1);[m
[31m-    transform-origin: 100% 0;[m
[31m-}[m
[31m-[m
[31m-.book-opening.is-playing .cover.turn {[m
[31m-    animation: bookCover 0.85s cubic-bezier(0.22, 0.8, 0.3, 1) forwards;[m
[31m-    animation-delay: var(--book-cover-delay);[m
[31m-}[m
[31m-[m
[31m-.book-opening.is-playing .page.turn {[m
[31m-    animation: bookOpen 3s forwards;[m
[31m-    animation-delay: var(--book-page-delay);[m
[31m-}[m
[31m-[m
[31m-.book-opening.is-playing .page:nth-of-type(1) {[m
[31m-    animation-delay: calc(var(--book-page-delay) + 0.05s);[m
[31m-}[m
[31m-[m
[31m-.book-opening.is-playing .page:nth-of-type(2) {[m
[31m-    animation-delay: calc(var(--book-page-delay) + 0.33s);[m
[32m+[m[32m    width: 100%;[m
[32m+[m[32m    height: 100%;[m
[32m+[m[32m    object-fit: contain;[m
[32m+[m[32m    background: transparent;[m
[32m+[m[32m    opacity: 0;[m
[32m+[m[32m    visibility: hidden;[m
[32m+[m[32m    pointer-events: none;[m
 }[m
 [m
[31m-.book-opening.is-playing .page:nth-of-type(3) {[m
[31m-    animation-delay: calc(var(--book-page-delay) + 0.66s);[m
[32m+[m[32m/* Fallback: chroma keying unavailable - show the raw video unmodified. */[m
[32m+[m[32m.book-opening.is-fallback .book-opening-video {[m
[32m+[m[32m    opacity: 1;[m
[32m+[m[32m    visibility: visible;[m
 }[m
 [m
[31m-.book-opening.is-playing .page:nth-of-type(4) {[m
[31m-    animation: bookOpen150deg 3s forwards;[m
[31m-    animation-delay: calc(var(--book-page-delay) + 0.99s);[m
[32m+[m[32m.book-opening-canvas {[m
[32m+[m[32m    position: absolute;[m
[32m+[m[32m    inset: 0;[m
[32m+[m[32m    z-index: 1;[m
[32m+[m[32m    display: block;[m
[32m+[m[32m    width: 100%;[m
[32m+[m[32m    height: 100%;[m
[32m+[m[32m    object-fit: contain;[m
[32m+[m[32m    background: transparent;[m
[32m+[m[32m    opacity: 0;[m
[32m+[m[32m    visibility: hidden;[m
[32m+[m[32m    pointer-events: none;[m
 }[m
 [m
[31m-.book-opening.is-playing .page:nth-of-type(5) {[m
[31m-    animation: bookOpen30deg 3s forwards;[m
[31m-    animation-delay: calc(var(--book-page-delay) + 1.2s);[m
[32m+[m[32m/* While keying, only the processed canvas is presented. */[m
[32m+[m[32m.book-opening.is-chroma .book-opening-canvas {[m
[32m+[m[32m    opacity: 1;[m
[32m+[m[32m    visibility: visible;[m
[32m+[m[32m    animation: bookOpeningPrelude 0.45s cubic-bezier(0.22, 0.8, 0.3, 1) both;[m
 }[m
 [m
[31m-.book-opening.is-playing .page:nth-of-type(6) {[m
[31m-    animation: bookOpen55deg 3s forwards;[m
[31m-    animation-delay: calc(var(--book-page-delay) + 1.25s);[m
[32m+[m[32m.book-opening.is-fallback .book-opening-canvas {[m
[32m+[m[32m    display: none;[m
 }[m
 [m
 @keyframes bookOpeningPrelude {[m
     from {[m
         opacity: 0;[m
[31m-        transform: translateY(0.8rem) scale(0.92);[m
[32m+[m[32m        transform: scale(0.96);[m
     }[m
     to {[m
         opacity: 1;[m
[31m-        transform: translateY(0) scale(1);[m
[31m-    }[m
[31m-}[m
[31m-[m
[31m-@keyframes bookOpen {[m
[31m-    30% {[m
[31m-        z-index: 999;[m
[31m-    }[m
[31m-    100% {[m
[31m-        z-index: 999;[m
[31m-        transform: rotateY(180deg);[m
[31m-    }[m
[31m-}[m
[31m-[m
[31m-@keyframes bookCover {[m
[31m-    30% {[m
[31m-        z-index: 999;[m
[31m-    }[m
[31m-    100% {[m
[31m-        z-index: 1;[m
[31m-        transform: rotateY(180deg);[m
[31m-    }[m
[31m-}[m
[31m-[m
[31m-@keyframes bookOpen150deg {[m
[31m-    30% {[m
[31m-        z-index: 999;[m
[31m-    }[m
[31m-    100% {[m
[31m-        z-index: 999;[m
[31m-        transform: rotateY(150deg);[m
[31m-    }[m
[31m-}[m
[31m-[m
[31m-@keyframes bookOpen55deg {[m
[31m-    30% {[m
[31m-        z-index: 999;[m
[31m-    }[m
[31m-    100% {[m
[31m-        z-index: 999;[m
[31m-        transform: rotateY(55deg);[m
[31m-    }[m
[31m-}[m
[31m-[m
[31m-@keyframes bookOpen30deg {[m
[31m-    50% {[m
[31m-        z-index: 999;[m
[31m-    }[m
[31m-    100% {[m
[31m-        z-index: 999;[m
[31m-        transform: rotateY(30deg);[m
[32m+[m[32m        transform: scale(1);[m
     }[m
 }[m
 [m
 @media (max-width: 480px) {[m
[31m-    .book-opening {[m
[31m-        padding: 0.6rem;[m
[31m-    }[m
[31m-[m
[31m-    .book-opening-panel {[m
[31m-        height: min(21rem, calc(100vh - 1.2rem));[m
[31m-        border-radius: 14px;[m
[32m+[m[32m    .book-opening-label {[m
[32m+[m[32m        font-size: 0.72rem;[m
[32m+[m[32m        bottom: 0.7rem;[m
     }[m
[31m-[m
 }[m
 [m
 @media (prefers-reduced-motion: reduce) {[m
     .yearbook-book.is-opening .yearbook-cover,[m
[31m-    .book-opening.is-playing .book,[m
[31m-    .book-opening.is-playing .cover.turn,[m
[31m-    .book-opening.is-playing .page.turn {[m
[32m+[m[32m    .book-opening.is-chroma .book-opening-canvas {[m
         animation: none;[m
     }[m
 }[m
[1mdiff --git a/FRONT/scslaicares.html b/FRONT/scslaicares.html[m
[1mindex 71edfb2..d1e782c 100644[m
[1m--- a/FRONT/scslaicares.html[m
[1m+++ b/FRONT/scslaicares.html[m
[36m@@ -123,17 +123,10 @@[m
                     <div class="book-opening" id="book-opening" role="dialog" aria-modal="true" aria-labelledby="book-opening-label" aria-hidden="true" hidden>[m
                         <div class="book-opening-panel">[m
                             <p class="book-opening-label" id="book-opening-label" data-book-opening-label>Opening yearbook</p>[m
[31m-                            <div class="book" aria-hidden="true">[m
[31m-                                <span class="page turn"></span>[m
[31m-                                <span class="page turn"></span>[m
[31m-                                <span class="page turn"></span>[m
[31m-                                <span class="page turn"></span>[m
[31m-                                <span class="page turn"></span>[m
[31m-                                <span class="page turn"></span>[m
[31m-                                <span class="cover"></span>[m
[31m-                                <span class="page"></span>[m
[31m-                                <span class="cover turn"></span>[m
[31m-                            </div>[m
[32m+[m[32m                            <video class="book-opening-video" id="book-opening-video" playsinline muted preload="auto" aria-hidden="true">[m
[32m+[m[32m                                <source src="animation/マエット%20(Maetto).mp4" type="video/mp4">[m
[32m+[m[32m                                Your browser does not support the video tag.[m
[32m+[m[32m                            </video>[m
                         </div>[m
                     </div>[m
     <footer>[m
